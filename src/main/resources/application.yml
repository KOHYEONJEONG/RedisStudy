spring:
  profiles:
    default: local
  datasource:
    url: jdbc:mysql://localhost:3306/mydb #데이터베이스 : mydb
    username: root
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql : true
